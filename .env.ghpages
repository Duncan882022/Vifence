# URL ngrok backend — bake vào GitHub Pages build (npm run build:pages).
# Cập nhật khi restart ngrok free, rồi push + redeploy Pages.
VITE_MOBILE_AI_BACKEND_URL=https://overrun-pavestone-gout.ngrok-free.dev
