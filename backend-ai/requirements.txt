fastapi>=0.115
uvicorn[standard]>=0.30
opencv-python>=4.9
ultralytics>=8.3.50
huggingface_hub>=0.24
hf_xet>=1.0  # bắt buộc để tải file từ repo HF dùng Xet storage (vd model cháy nổ/hút thuốc ở trên)
pydantic>=2.7
pydantic-settings>=2.4
eval_type_backport>=0.2  # cho phép pydantic hiểu cú pháp "X | None" trên Python < 3.10
python-dotenv>=1.0
numpy>=1.26
torch>=2.0
# openvino>=2024.4  # bật khi export/load YOLOv8s OpenVINO (scripts/export_person_openvino.py)
transformers>=4.40  # OWLv2 zero-shot — detect máy móc DZ (Cam A-04) theo mô tả text, không rule màu/vị trí cố định
